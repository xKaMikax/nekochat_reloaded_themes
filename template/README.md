# Theme template

Copy this folder, rename it, and start editing `theme.css`.

It already works as-is (it looks like Windows Classic), so you can install it right away and see your changes step by step.

See the full guide: [Creating themes](../docs/CREATING_THEMES.md).
